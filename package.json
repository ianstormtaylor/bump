{
  "name": "set-version",
  "description": "Easily update the version of all the different package.json equivalents.",
  "repository": "git://github.com/ianstormtaylor/set-version.git",
  "version": "0.0.1",
  "license": "MIT",
  "bin": {
    "v": "bin/set-version"
  },
  "dependencies": {
    "listify": "0.0.1",
    "colors": "~0.6.2"
  },
  "devDependencies": {
    "mocha": "1.x"
  }
}
